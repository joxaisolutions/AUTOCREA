# ❓ FAQ y Troubleshooting - JoxCoder en AUTOCREA

## Preguntas Frecuentes

### 1. ¿Cómo obtengo mi API Key de Relevance AI?

**Respuesta:**
1. Ve a [Relevance AI Dashboard](https://relevance.ai)
2. Haz clic en tu perfil (esquina superior derecha)
3. Selecciona **Settings** → **API Keys**
4. Si no tienes una, haz clic en **Create new API Key**
5. Copia la key y guárdala en un lugar seguro

⚠️ **Importante:** La API Key solo se muestra una vez. Si la pierdes, deberás crear una nueva.

---

### 2. ¿Cómo encuentro mi Agent ID?

**Respuesta:**
1. Ve a tu lista de agentes en Relevance AI
2. Haz clic en JoxCoder
3. Mira la URL del navegador, verás algo como:
   ```
   https://relevance.ai/agents/agent_abc123xyz
   ```
4. El Agent ID es la parte después de `/agents/`: `agent_abc123xyz`

**Alternativa:**
- En la página del agente, busca la sección "Integration" o "API"
- El Agent ID debería estar visible ahí

---

### 3. ¿Puedo usar JoxCoder sin autenticación de usuarios?

**Respuesta:**
Sí, hay dos opciones:

**Opción A: Sin autenticación (público)**
```typescript
// En app/api/joxcoder/chat/route.ts
// Comenta o elimina esta sección:
/*
const supabase = createServerComponentClient({ cookies });
const { data: { session } } = await supabase.auth.getSession();

if (!session) {
  return NextResponse.json(
    { success: false, error: 'No autorizado' },
    { status: 401 }
  );
}
*/
```

**Opción B: Autenticación con API Key propia**
```typescript
// Valida un API key en los headers
const apiKey = request.headers.get('x-api-key');
if (apiKey !== process.env.YOUR_INTERNAL_API_KEY) {
  return NextResponse.json(
    { success: false, error: 'API Key inválida' },
    { status: 401 }
  );
}
```

⚠️ **Advertencia:** Sin autenticación, cualquiera puede usar tu API y consumir tus créditos de Relevance AI.

---

### 4. ¿Cuánto cuesta usar JoxCoder?

**Respuesta:**
Los costos dependen de tu plan en Relevance AI:

- **Tokens usados:** Cada mensaje consume tokens según su longitud
- **Llamadas API:** Algunas cuentas tienen límite de llamadas/mes
- **Plan gratuito:** Usualmente incluye 10,000-50,000 tokens/mes

**Cómo verificar tu uso:**
1. Dashboard de Relevance AI → Usage
2. Implementa analytics en AUTOCREA (ver guía de instalación)

**Consejo:** Implementa rate limiting para evitar costos inesperados.

---

### 5. ¿JoxCoder puede acceder a mis archivos en AUTOCREA?

**Respuesta:**
No automáticamente. Debes enviar los archivos explícitamente:

```typescript
// Ejemplo: Enviar código de un archivo al chat
const fileContent = await readFile('path/to/file.tsx');

const response = await joxcoder.chat(
  '¿Puedes revisar este código?',
  {
    files: [
      { name: 'component.tsx', content: fileContent }
    ]
  }
);
```

Para integración automática, necesitarías:
1. Detector de archivos abiertos en tu editor
2. Sistema para adjuntar automáticamente el contexto
3. Permisos del usuario para compartir código

---

### 6. ¿Puedo personalizar las respuestas de JoxCoder?

**Respuesta:**
Sí, de varias formas:

**En Relevance AI:**
- Edita las instrucciones del agente
- Modifica el prompt del sistema
- Agrega ejemplos de respuestas deseadas

**En tu código:**
```typescript
// Agrega contexto específico antes de cada mensaje
const enrichedMessage = `
Contexto: Estamos usando Next.js 14 con App Router
Stack: ${projectStack}
Preferencias: Código en TypeScript, siempre con manejo de errores

Usuario pregunta: ${userMessage}
`;
```

---

### 7. ¿Las conversaciones se guardan automáticamente?

**Respuesta:**
Depende de tu implementación:

- **Si creaste la tabla de Supabase:** Sí, se guardan (ver paso 4 de la guía)
- **Si no:** No, solo se mantienen en memoria durante la sesión

Para habilitar guardado:
1. Crea la tabla en Supabase (ver guía SQL)
2. Descomenta la sección de guardado en el API route
3. Configura RLS (Row Level Security) para privacidad

---

### 8. ¿Puedo usar JoxCoder con otros modelos (GPT-4, Gemini, etc.)?

**Respuesta:**
No directamente. JoxCoder está configurado específicamente en Relevance AI.

**Alternativas:**
1. **Crear múltiples agentes:** Un agente por modelo en Relevance
2. **Cambiar de plataforma:** Migrar a una solución que soporte múltiples modelos
3. **Wrapper personalizado:** Crear tu propia abstracción que permita cambiar backends

---

### 9. ¿Qué hago si JoxCoder da respuestas incorrectas?

**Respuesta:**

1. **Revisa el prompt:**
   - ¿Está claro y específico?
   - ¿Incluiste contexto suficiente?

2. **Mejora las instrucciones del agente en Relevance:**
   - Agrega ejemplos de respuestas correctas
   - Especifica el formato de salida deseado
   - Incluye casos edge que debe considerar

3. **Feedback loop:**
   ```typescript
   // Implementa sistema de feedback
   const sendFeedback = async (messageId: string, isHelpful: boolean) => {
     await supabase.from('joxcoder_feedback').insert({
       message_id: messageId,
       helpful: isHelpful,
       timestamp: new Date()
     });
   };
   ```

4. **Re-entrena o ajusta:** Usa los feedbacks para mejorar el agente

---

### 10. ¿Puedo usar JoxCoder en producción?

**Respuesta:**
Sí, pero considera:

✅ **Antes de lanzar:**
- [ ] Implementa rate limiting robusto
- [ ] Configura monitoreo de errores (Sentry, etc.)
- [ ] Establece límites de uso por usuario
- [ ] Implementa caché para preguntas comunes
- [ ] Configura alertas para uso anómalo
- [ ] Prueba bajo carga (stress testing)
- [ ] Documenta el comportamiento esperado
- [ ] Ten un plan de contingencia si Relevance tiene downtime

🔒 **Seguridad:**
- Valida y sanitiza todos los inputs
- No expongas API keys en el frontend
- Implementa CORS correctamente
- Usa HTTPS siempre
- Protege endpoints con autenticación

📊 **Costos:**
- Calcula el costo promedio por usuario
- Establece presupuesto mensual
- Monitorea uso en tiempo real
- Considera plan empresarial si es necesario

---

## Errores Comunes y Soluciones

### Error: `TypeError: fetch failed`

**Causa:** Problemas de red o URL incorrecta

**Solución:**
```typescript
// Verifica la URL base en .env
RELEVANCE_BASE_URL=https://api-bcbe99.stack.tryrelevance.com/latest

// Si estás en Europa, prueba:
RELEVANCE_BASE_URL=https://api-eu.relevance.ai/latest
```

---

### Error: `401 Unauthorized`

**Causa:** API Key inválida o expirada

**Soluciones:**
1. Verifica que la key esté correcta (sin espacios)
2. Genera una nueva API Key en Relevance
3. Confirma que la key tenga permisos suficientes

```bash
# Test rápido
curl -H "Authorization: Bearer TU_API_KEY" \
  https://api-bcbe99.stack.tryrelevance.com/latest/agents
```

---

### Error: `429 Too Many Requests`

**Causa:** Excediste el límite de llamadas

**Solución:**
```typescript
// Implementa exponential backoff
async function retryWithBackoff(fn: () => Promise<any>, maxRetries = 3) {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await fn();
    } catch (error) {
      if (i === maxRetries - 1) throw error;
      const delay = Math.pow(2, i) * 1000; // 1s, 2s, 4s
      await new Promise(resolve => setTimeout(resolve, delay));
    }
  }
}
```

---

### Error: `Conversation context too long`

**Causa:** El historial de conversación excede el límite de tokens

**Solución:**
```typescript
// Limita el historial a los últimos N mensajes
const MAX_HISTORY_LENGTH = 10;

if (conversationHistory.length > MAX_HISTORY_LENGTH) {
  conversationHistory = conversationHistory.slice(-MAX_HISTORY_LENGTH);
}

// O implementa resumen del historial
const summarizeHistory = async (history: Message[]) => {
  // Usa JoxCoder para resumir el historial
  const summary = await joxcoder.chat(
    `Resume esta conversación en 2-3 oraciones: ${JSON.stringify(history)}`
  );
  return summary.response;
};
```

---

### Las respuestas tardan más de 30 segundos

**Causa:** Prompts muy largos o modelo sobrecargado

**Soluciones:**
1. **Reduce el tamaño del prompt:**
   - Elimina información redundante
   - Resume archivos largos
   - Usa contexto más específico

2. **Implementa timeout:**
   ```typescript
   const TIMEOUT = 30000; // 30 segundos

   const responsePromise = joxcoder.chat(message);
   const timeoutPromise = new Promise((_, reject) =>
     setTimeout(() => reject(new Error('Timeout')), TIMEOUT)
   );

   const response = await Promise.race([responsePromise, timeoutPromise]);
   ```

3. **Divide en múltiples llamadas:**
   - Para tareas complejas, divide en pasos
   - Procesa cada paso por separado

---

### El componente no se renderiza

**Causa:** Error en imports o configuración de Tailwind

**Solución:**
```typescript
// Verifica que Tailwind esté configurado
// tailwind.config.js
module.exports = {
  content: [
    './app/**/*.{js,ts,jsx,tsx}',
    './components/**/*.{js,ts,jsx,tsx}',
  ],
  // ... resto de la config
}

// Verifica imports en el componente
import { Send, Loader2, Bot } from 'lucide-react'; // ✅
import { Send } from '@lucide/react'; // ❌ Incorrecto
```

---

## Contacto y Soporte

**Relevance AI:**
- Email: support@relevance.ai
- Docs: https://docs.relevance.ai
- Discord: https://discord.gg/relevance

**Tu equipo de AUTOCREA:**
- Revisa la documentación interna
- Crea un ticket en tu sistema de soporte

---

**¿Tienes una pregunta que no está aquí?**
Contacta al equipo de desarrollo de AUTOCREA o consulta la documentación de Relevance AI.